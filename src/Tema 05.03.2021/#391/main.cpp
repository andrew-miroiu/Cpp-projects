#include <iostream>

using namespace std;

int main()
{
    int a, b, m, n, c, d;
    int cmd=1;
    int n, x=0;
    cin>>n;

    for(int x=1; x<=n; x++)
    {
        cin>>a;
        cin>>b;

        m=a*

        for(int d=1; d<=min(a,b); d++)
        {
            if(m%d==0 && n%d==0)
            {
                cmmdc1=d;
            }
        }
    }

    return 0;
}
